# 开发环境配置
#
# BACKEND_TARGET
#   dev server 收到 /api 请求时转发到的后端地址（仅用于 vite.config.js 中的 proxy）
#   - 留空则使用默认值 http://localhost:8080
#   - 也可通过命令行临时覆盖：BACKEND_TARGET=http://staging:8080 npm run dev
#   - 还可通过 .env.development.local（git 忽略）针对本机定制
#
# VITE_API_BASE_URL
#   axios 在浏览器中发请求时的 baseURL（相对路径 /api，由 dev server 代理转发）
#   - 留空 = 走相对路径，由 Vite proxy 转发
#   - 一般无需修改，除非你想让浏览器直连某个绝对地址（不推荐）

BACKEND_TARGET=http://localhost:8080
VITE_API_BASE_URL=